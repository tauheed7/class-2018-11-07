# Scratch file for working with the files in data.

library(tidyverse)
library(stringr)
library(fs)

# 1. Create a vector with all the file names in data/.

# 2. Create a vector with just the file names that have an "A" in them.

# 3. Read data/ex_926_I.csv into a tibble and provide a summary.

# 4. Read in all the files into one big tibble. Check out ?map_dfr . . .

# 5. Find the 4 files with the largest number of observations.